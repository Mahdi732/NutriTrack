<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Sign In — Nutrition Plan</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            serif: ['Georgia', 'Cambria', 'Times New Roman', 'Times', 'serif'],
          }
        }
      }
    }
  </script>
</head>
<body class="min-h-screen bg-stone-50 flex items-center justify-center p-6">
  
  <main class="w-full max-w-md bg-white border border-stone-200 p-8">
    <header class="text-center mb-8">
      <h1 class="text-4xl font-serif font-bold text-stone-900 mb-2">Nutrition Plan</h1>
      <p class="text-stone-600">Sign in to access your program</p>
    </header>

    <form action="/auth/login" method="post" class="space-y-6" aria-label="Sign in form">
      <div>
        <label for="email" class="block text-sm font-medium text-stone-700 mb-2">Email</label>
        <input id="email" name="email" type="email" required
               class="block w-full border border-stone-300 px-4 py-2 text-stone-900 focus:outline-none focus:border-stone-900 transition-colors" 
               placeholder="your@email.com">
      </div>

      <div>
        <label for="password" class="block text-sm font-medium text-stone-700 mb-2">Password</label>
        <input id="password" name="password" type="password" required
               class="block w-full border border-stone-300 px-4 py-2 text-stone-900 focus:outline-none focus:border-stone-900 transition-colors" 
               placeholder="••••••••">
      </div>

      <% if (error) { %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
            <%= error %>
        </div>
      <% } %>

      <div class="flex items-center justify-between text-sm">
        <label class="inline-flex items-center gap-2">
          <input type="checkbox" name="remember" class="h-4 w-4 border-stone-300 text-stone-900">
          <span class="text-stone-600">Remember me</span>
        </label>
      </div>

      <button type="submit"
              class="w-full bg-stone-900 text-white py-3 font-semibold hover:bg-stone-800 focus:outline-none focus:ring-2 focus:ring-stone-900 focus:ring-offset-2 transition-colors">
        Sign In
      </button>
    </form>

    <div class="mt-8 pt-6 border-t border-stone-200 text-center text-sm text-stone-600">
      New here?
      <a href="/auth/register" class="text-stone-900 font-semibold hover:underline ml-1">Create an account</a>
    </div>
  </main>
</body>
</html>
