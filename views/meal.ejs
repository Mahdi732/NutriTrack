<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nutrition Analysis - Pancake Breakfast</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        serif: ['Playfair Display', 'serif'],
                        sans: ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-stone-50 font-sans text-stone-900">
    <header class="border-b border-stone-200 bg-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
            <div class="flex items-center justify-between">
                <div>
                    <h1 class="text-4xl font-serif font-bold text-stone-900">Nutrition Analysis</h1>
                    <p class="text-stone-600 mt-2">Detailed breakdown of your meal</p>
                </div>
                <nav class="space-x-6 text-sm">
                    <a href="/" class="text-stone-900 hover:text-stone-600 transition">Home</a>
                    <a href="/meal" class="text-stone-600 hover:text-stone-900 transition">Analysis</a>
                    <a href="/rapport" class="text-stone-600 hover:text-stone-900 transition">Reports</a>
                    <% if (user) { %>
                    <a href="/profile" class="text-stone-600 hover:text-stone-900 transition">Profile</a>
                    <% } else { %>
                    <a href="/auth/login" class="px-4 py-2 border border-stone-300 rounded hover:bg-stone-50 transition">Login</a>
                    <% } %>
                </nav>
            </div>
        </div>
    </header>

    <main id="main" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <section class="bg-white border border-stone-200 p-8 mb-12">
            <div class="grid md:grid-cols-2 gap-8">
                <div class="flex flex-col">
                    <h2 class="text-2xl font-serif font-semibold text-stone-900 mb-4">Food Image</h2>
                    <div class="flex-1 bg-stone-100 border-2 border-dashed border-stone-300 rounded-lg flex items-center justify-center min-h-[300px] overflow-hidden">
                        <img src="https://img.freepik.com/vecteurs-premium/icone-attente-element-signe-plat-collection-gestion-du-temps-icone-attente-creative-pour-infographie-modeles-conception-web-plus-encore_676904-2468.jpg" alt="Food" id="analysisImage" class="w-full h-full rounded-lg object-cover">
                    </div>
                </div>

                <div class="flex flex-col">
                    <h2 class="text-2xl font-serif font-semibold text-stone-900 mb-4">Upload New Image</h2>
                    <form id="analysisForm" enctype="multipart/form-data" class="flex-1 flex flex-col">
                        <div class="flex-1 flex flex-col justify-center space-y-4">
                            <div>
                                <label for="food-image" class="block text-sm font-medium text-stone-700 mb-2">
                                    Select food image
                                </label>
                                <input type="file" id="food-image" name="mealImage" accept="image/*" required class="block w-full text-sm text-stone-500 file:mr-4 file:py-3 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-stone-900 file:text-white hover:file:bg-stone-800 file:cursor-pointer cursor-pointer">
                            </div>
                            <button type="submit" id="analysisButton" class="bg-stone-900 hover:bg-stone-800 text-white font-semibold py-3 px-6 rounded-lg flex items-center justify-center transition-colors">
                                Analyze Nutrition
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </section>

        <div id="analysisContainer">

        </div>
    </main>

    <footer class="border-t border-stone-200 bg-white mt-16">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <p class="text-center text-stone-500 text-sm">Nutrition Analysis Tool © 2025</p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="/meal.public.js"></script>
</body>
</html>